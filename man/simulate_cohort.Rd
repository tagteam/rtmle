% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_cohort.R
\name{simulate_cohort}
\alias{simulate_cohort}
\title{Simulate a longitudinal event-history cohort.}
\usage{
simulate_cohort(
  n,
  seed = NULL,
  max_follow,
  baseline_variables = NULL,
  post_baseline_variables_hook = NULL,
  baseline_visit = NULL,
  post_baseline_visit_hook = NULL,
  visit_schedule = NULL,
  visit_events = NULL,
  visit_measurements = NULL,
  post_visit_hook = NULL,
  intermediate_events = NULL,
  intermediate_events_hook = NULL,
  absorbing_events,
  absorbing_events_hook = NULL,
  parameter_values,
  intervention = NULL,
  regime = NULL
)
}
\arguments{
\item{n}{Number of subjects.}

\item{seed}{Optional random seed.}

\item{max_follow}{Maximum follow-up time on the internal visit scale.}

\item{baseline_variables}{Named list describing baseline variable
distributions.}

\item{post_baseline_variables_hook}{Optional hook applied after baseline
variables are simulated.}

\item{baseline_visit}{Optional named list describing baseline visit variables.}

\item{post_baseline_visit_hook}{Optional hook applied after baseline visit
variables are simulated.}

\item{visit_schedule}{Named list controlling the visit gap distribution.}

\item{visit_events}{Named list describing time-updated treatment variables.}

\item{visit_measurements}{Named list describing time-updated measurements.}

\item{post_visit_hook}{Optional hook applied after each visit update.}

\item{intermediate_events}{Named list describing non-absorbing events.}

\item{intermediate_events_hook}{Optional hook to alter the intermediate-event
model over time.}

\item{absorbing_events}{Named list describing absorbing events.}

\item{absorbing_events_hook}{Optional hook to alter the absorbing-event model
over time.}

\item{parameter_values}{Named list of model parameters.}

\item{intervention}{Optional named list of constant intervention values.}

\item{regime}{Reserved for future use.}
}
\value{
A longitudinal event-history \code{\link[data.table]{data.table}}.
}
\description{
Simulates a configurable longitudinal event-history cohort. The simulator is
adapted from \code{tagteam/followme/functions/simulate_cohort.R}.
}
\examples{
library(lava)
library(data.table)
simulate_cohort(
n = 3,
seed = 977,
max_follow = 20,
baseline_variables = list(age = "normal",sex = "binomial",BMI = "normal"),
baseline_visit = list(treatment = "binomial"),
visit_schedule = list(mean = 1,sd = 0.1,skip = 0),
visit_events = list(treatment = "binomial"),
visit_measurements = list("changeBMI" = "normal"),
absorbing_events = list(death = "Weibull",outcome = "Weibull",dropout = "Weibull"),
intermediate_events = list(stroke = "Weibull"),
parameter_values = list(
intercept_sex=.3,
intercept_age=60,
var_age=10,
intercept_BMI=30,
var_BMI=3,
intercept_treatment=0.3,
effect_treatment_death = -0.7,
effect_treatment_outcome = 0.1,
effect_treatment_dropout = 0,
effect_treatment_changeBMI = 0.5,
scale_death=0.01,
scale_outcome=0.03,
scale_stroke=0.02,
scale_dropout = 0.04
)
)

}
\seealso{
\code{\link{simulate_long_data}}, \code{\link{long_to_wide}}
}
